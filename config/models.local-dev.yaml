# config/models.local-dev.yaml
ingestion:
  parser: simple
retrieval:
  use_visual: false
  use_rerank: true
llm:
  base_url: https://api.openai.com/v1
  api_key: ${OPENAI_API_KEY}
  model: gpt-4o-mini

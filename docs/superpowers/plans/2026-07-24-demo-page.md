# PrismRAG Demo 页 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 Hybrid 静态 Demo：默认 Fixture 可离线展示问答/引用/三路 Trace/工程师面板/评测角标；Live 可 `/ask` + `/ingest`，挂载于 FastAPI `/demo/`。

**Architecture:** 零 Node。`static/demo/` 单页（`index.html` + `styles.css` + `app.js` + JSON 数据）由 `StaticFiles` 挂到已有 `src/api/routes.py`。Demo 模式读本地 JSON；Live 模式 `fetch` 现有 `/health` `/ask` `/ingest`，解析 `X-Trace-Id`。不改 `AskResponse` schema。

**Tech Stack:** FastAPI、StaticFiles、原生 HTML/CSS/JS、pytest + TestClient、仓库现有 `/ask` 契约。

**Spec:** [`docs/superpowers/specs/2026-07-24-demo-page-design.md`](../specs/2026-07-24-demo-page-design.md)

**Branch:** 实现用 `feat/demo-page`（从当前 `docs/demo-page-design` 或 `main` 切出；**禁止直接在 `main` 改代码**）。

**DoD（验收）:**
1. Demo 模式打开 `/demo/`，点预设 ≤3s 出答案 + citations + 三路 top5 + 可展开工程面板
2. Live：health 指示 + ask 成功 + Trace-Id 展示
3. Live：上传 PDF → doc_id；可选「仅当前文档」再 ask
4. 角标含 NDCG/E2E 等可辩护口径说明
5. `pytest tests/test_api.py tests/test_demo_fixtures.py -q` 通过；现有 API 测不回归

**明确不做:** 纯 `/search` 模式、React/Node、实时评测管道、默认开 CRAG/Gate2、jsdom 全量前端测。

---

## File map

| 路径 | 职责 |
|------|------|
| `static/demo/index.html` | 左右分栏 DOM 骨架 |
| `static/demo/styles.css` | 顶栏/分栏/卡片样式 |
| `static/demo/app.js` | 状态、Demo/Live、渲染、上传 |
| `static/demo/fixtures.json` | presets + AskResponse 形 responses |
| `static/demo/metrics.json` | 角标 chips + 口径 |
| `src/api/routes.py` | 文件末尾 mount `/demo`（路径相对仓库根） |
| `tests/test_demo_fixtures.py` | fixtures/metrics JSON 契约 |
| `tests/test_api.py` | 增补 `GET /demo/` 或 `/demo/index.html` 200 烟测 |
| `README.md` | 简短「打开 Demo」一节（可选但推荐） |

---

### Task 1: Fixture + metrics 数据与契约测试

**Files:**
- Create: `static/demo/fixtures.json`
- Create: `static/demo/metrics.json`
- Create: `tests/test_demo_fixtures.py`

- [ ] **Step 1: 写失败的契约测试**

```python
# tests/test_demo_fixtures.py
"""static/demo JSON 契约：可解析且与 AskResponse 字段对齐。"""
from __future__ import annotations

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
DEMO = ROOT / "static" / "demo"


def test_fixtures_file_exists_and_parses():
    path = DEMO / "fixtures.json"
    assert path.is_file(), f"missing {path}"
    data = json.loads(path.read_text(encoding="utf-8"))
    assert "presets" in data and "responses" in data
    assert len(data["presets"]) >= 4


def test_each_preset_has_matching_response():
    data = json.loads((DEMO / "fixtures.json").read_text(encoding="utf-8"))
    for p in data["presets"]:
        q = p["query"]
        assert q in data["responses"], f"preset query missing response: {q!r}"
        resp = data["responses"][q]
        assert "answer" in resp
        assert "citations" in resp
        assert "retrieval_trace" in resp
        rt = resp["retrieval_trace"]
        for key in ("bm25_top5", "dense_top5", "visual_top5"):
            assert key in rt
            assert isinstance(rt[key], list)


def test_has_reject_style_fixture():
    data = json.loads((DEMO / "fixtures.json").read_text(encoding="utf-8"))
    answers = [r["answer"].lower() for r in data["responses"].values()]
    assert any(
        "enough information" in a or "cannot answer" in a or "not enough" in a
        for a in answers
    ), "need at least one abstain/reject-style answer for demo storytelling"


def test_metrics_chips():
    path = DEMO / "metrics.json"
    assert path.is_file()
    data = json.loads(path.read_text(encoding="utf-8"))
    assert len(data["chips"]) >= 2
    for c in data["chips"]:
        assert c.get("label") and c.get("value") and c.get("detail")
```

- [ ] **Step 2: 跑测确认失败**

```bash
cd /Users/theyang/Documents/ai/pdf-rag
.venv/bin/python -m pytest tests/test_demo_fixtures.py -v
```

Expected: FAIL（`static/demo` 不存在或 missing file）

- [ ] **Step 3: 写入 `metrics.json`**

```json
{
  "chips": [
    {
      "label": "NDCG@10",
      "value": "0.53",
      "detail": "Boot-A · Full_zerank2 · 283q en · 协议 v1 · runs/20260720-bootA（Full_no_rerank 0.42 → zerank2 0.53）"
    },
    {
      "label": "E2E Correct",
      "value": "0.66",
      "detail": "Goal-A ON 索引 · runs/20260723-on-goalA · 与拒答口径见 src/rejection.py"
    },
    {
      "label": "Reject acc.",
      "value": "0.95",
      "detail": "E2E 拒答题准确率 · 同 Goal-A / handoff 定稿数字"
    }
  ],
  "footnote": "角标为归档可辩护数字，非本页实时评测。CRAG 默认关闭，勿写质量大涨。"
}
```

- [ ] **Step 4: 写入 `fixtures.json`（合成工业手册风格，字段对齐 AskResponse）**

完整文件（实现时原样创建；query 字符串必须与 presets 完全一致）：

```json
{
  "presets": [
    {
      "id": "q_voltage",
      "label": "Rated voltage",
      "query": "What is the rated supply voltage for the main control panel?"
    },
    {
      "id": "q_procedure",
      "label": "Pre-check steps",
      "query": "List the pre-operation checks before starting pressurized fuel transfer."
    },
    {
      "id": "q_multi",
      "label": "Multi-citation PSI",
      "query": "What is the maximum water pressure allowed for manual rinsing?"
    },
    {
      "id": "q_reject",
      "label": "Out-of-corpus",
      "query": "What is the stock price of the equipment manufacturer next quarter?"
    },
    {
      "id": "q_gate2",
      "label": "Gate2 sample",
      "query": "What torque is specified for the braid clamp fasteners?"
    }
  ],
  "responses": {
    "What is the rated supply voltage for the main control panel?": {
      "query": "What is the rated supply voltage for the main control panel?",
      "answer": "The main control panel is rated for 480 VAC, three-phase supply per the equipment data plate and Section 3.1 of the service manual.",
      "citations": [
        {
          "chunk_id": "demo-doc:p12:c0",
          "page_id": 12,
          "doc_id": "demo-doc",
          "page_number": 12,
          "snippet": "Main control panel rated supply: 480 VAC, 3-phase, 60 Hz. Do not connect to 208 VAC service without the step-up transformer kit."
        }
      ],
      "retrieval_trace": {
        "bm25_top5": [
          {"chunk_id": "demo-doc:p12:c0", "page_id": 12, "score": 12.4},
          {"chunk_id": "demo-doc:p12:c1", "page_id": 12, "score": 8.1},
          {"chunk_id": "demo-doc:p3:c0", "page_id": 3, "score": 5.2}
        ],
        "dense_top5": [
          {"chunk_id": "demo-doc:p12:c0", "page_id": 12, "score": 0.87},
          {"chunk_id": "demo-doc:p14:c2", "page_id": 14, "score": 0.71},
          {"chunk_id": "demo-doc:p3:c0", "page_id": 3, "score": 0.64}
        ],
        "visual_top5": [
          {"chunk_id": "page:12", "page_id": 12, "score": 0.66},
          {"chunk_id": "page:14", "page_id": 14, "score": 0.51}
        ]
      },
      "self_rag": {"enabled": false},
      "crag": {"enabled": false, "applied": false},
      "_demo_trace_id": "fixture-voltage-001"
    },
    "List the pre-operation checks before starting pressurized fuel transfer.": {
      "query": "List the pre-operation checks before starting pressurized fuel transfer.",
      "answer": "Before pressurized transfer: (1) verify high-level shutoff pre-test PASS, (2) confirm bonding cables attached, (3) inspect nozzle strainer in REFUEL position, (4) ensure wet/dry sensor indicators are GREEN on the main panel.",
      "citations": [
        {
          "chunk_id": "demo-doc:p40:c1",
          "page_id": 40,
          "doc_id": "demo-doc",
          "page_number": 40,
          "snippet": "Pre-operation checklist — pressurized transfer: high-level shutoff pretest; bonding; strainer position; wet/dry indicators GREEN."
        },
        {
          "chunk_id": "demo-doc:p41:c0",
          "page_id": 41,
          "doc_id": "demo-doc",
          "page_number": 41,
          "snippet": "Do not initiate fuel flow until all trucks on that aircraft side are bonded and the panel pretest completes without fault codes."
        }
      ],
      "retrieval_trace": {
        "bm25_top5": [
          {"chunk_id": "demo-doc:p40:c1", "page_id": 40, "score": 14.0},
          {"chunk_id": "demo-doc:p41:c0", "page_id": 41, "score": 11.2}
        ],
        "dense_top5": [
          {"chunk_id": "demo-doc:p40:c1", "page_id": 40, "score": 0.84},
          {"chunk_id": "demo-doc:p41:c0", "page_id": 41, "score": 0.79}
        ],
        "visual_top5": [
          {"chunk_id": "page:40", "page_id": 40, "score": 0.58}
        ]
      },
      "self_rag": {"enabled": false},
      "crag": {"enabled": false, "applied": false},
      "_demo_trace_id": "fixture-precheck-002"
    },
    "What is the maximum water pressure allowed for manual rinsing?": {
      "query": "What is the maximum water pressure allowed for manual rinsing?",
      "answer": "Manual rinsing must not exceed 175 PSI. Use the regulated wash wand; higher pressure may damage seals and sensor housings [1][2].",
      "citations": [
        {
          "chunk_id": "demo-doc:p88:c0",
          "page_id": 88,
          "doc_id": "demo-doc",
          "page_number": 88,
          "snippet": "Manual rinse: maximum water pressure 175 PSI (1206 kPa). Do not use fire-main pressure without a reducing valve."
        },
        {
          "chunk_id": "demo-doc:p89:c1",
          "page_id": 89,
          "doc_id": "demo-doc",
          "page_number": 89,
          "snippet": "Table 7-2 Wash limits — rinse wand 175 PSI max; foam lance 100 PSI max."
        }
      ],
      "retrieval_trace": {
        "bm25_top5": [
          {"chunk_id": "demo-doc:p88:c0", "page_id": 88, "score": 15.1},
          {"chunk_id": "demo-doc:p89:c1", "page_id": 89, "score": 13.0},
          {"chunk_id": "demo-doc:p90:c0", "page_id": 90, "score": 6.4}
        ],
        "dense_top5": [
          {"chunk_id": "demo-doc:p89:c1", "page_id": 89, "score": 0.91},
          {"chunk_id": "demo-doc:p88:c0", "page_id": 88, "score": 0.89}
        ],
        "visual_top5": [
          {"chunk_id": "page:89", "page_id": 89, "score": 0.72},
          {"chunk_id": "page:88", "page_id": 88, "score": 0.70}
        ]
      },
      "self_rag": {"enabled": false},
      "crag": {"enabled": false, "applied": false},
      "_demo_trace_id": "fixture-psi-003"
    },
    "What is the stock price of the equipment manufacturer next quarter?": {
      "query": "What is the stock price of the equipment manufacturer next quarter?",
      "answer": "I don't have enough information to answer that question.",
      "citations": [],
      "retrieval_trace": {
        "bm25_top5": [
          {"chunk_id": "demo-doc:p1:c0", "page_id": 1, "score": 2.1}
        ],
        "dense_top5": [
          {"chunk_id": "demo-doc:p2:c0", "page_id": 2, "score": 0.21}
        ],
        "visual_top5": []
      },
      "self_rag": {"enabled": false},
      "crag": {"enabled": false, "applied": false},
      "_demo_trace_id": "fixture-reject-004"
    },
    "What torque is specified for the braid clamp fasteners?": {
      "query": "What torque is specified for the braid clamp fasteners?",
      "answer": "Braid clamp fasteners are torqued to 25 in-lb ± 2 in-lb per the connector assembly table.",
      "citations": [
        {
          "chunk_id": "demo-doc:p55:c2",
          "page_id": 55,
          "doc_id": "demo-doc",
          "page_number": 55,
          "snippet": "Braid clamp fastener torque: 25 in-lb (2.8 N·m), tolerance ±2 in-lb. Recheck after thermal cycle."
        }
      ],
      "retrieval_trace": {
        "bm25_top5": [
          {"chunk_id": "demo-doc:p55:c2", "page_id": 55, "score": 11.0}
        ],
        "dense_top5": [
          {"chunk_id": "demo-doc:p55:c2", "page_id": 55, "score": 0.86}
        ],
        "visual_top5": [
          {"chunk_id": "page:55", "page_id": 55, "score": 0.61}
        ]
      },
      "self_rag": {
        "enabled": true,
        "passed": true,
        "score": 0.92,
        "attempts": 1,
        "final_action": "accept",
        "gate_degraded": false,
        "attempts_detail": [
          {
            "attempt": 1,
            "prompt_id": "gate2_v1",
            "answer": "Braid clamp fasteners are torqued to 25 in-lb ± 2 in-lb per the connector assembly table.",
            "action": "accept",
            "passed": true,
            "score": 0.92,
            "unsupported": [],
            "latency_ms": 180.0,
            "gate_degraded": false,
            "error": null
          }
        ]
      },
      "crag": {"enabled": false, "applied": false},
      "_demo_trace_id": "fixture-gate2-005"
    }
  }
}
```

- [ ] **Step 5: 跑测通过**

```bash
.venv/bin/python -m pytest tests/test_demo_fixtures.py -v
```

Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
git checkout -b feat/demo-page   # 若尚不在该分支
git add static/demo/fixtures.json static/demo/metrics.json tests/test_demo_fixtures.py
git commit -m "test(demo): add fixture/metrics JSON contracts and sample data"
```

---

### Task 2: 页面骨架 HTML + CSS（无逻辑）

**Files:**
- Create: `static/demo/index.html`
- Create: `static/demo/styles.css`

- [ ] **Step 1: 创建 `styles.css`**

```css
/* static/demo/styles.css */
:root {
  --bg: #f1f5f9;
  --panel: #ffffff;
  --ink: #0f172a;
  --muted: #64748b;
  --line: #e2e8f0;
  --accent: #2563eb;
  --top: #0f172a;
  --ok: #16a34a;
  --bad: #dc2626;
  --warn: #d97706;
  font-family: ui-sans-serif, system-ui, -apple-system, "Segoe UI", sans-serif;
}
* { box-sizing: border-box; }
body {
  margin: 0;
  background: var(--bg);
  color: var(--ink);
  min-height: 100vh;
}
.topbar {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  justify-content: space-between;
  padding: 12px 18px;
  background: var(--top);
  color: #f8fafc;
}
.brand strong { font-size: 1.05rem; }
.brand span { opacity: 0.7; margin-left: 8px; font-size: 0.85rem; }
.top-right { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; }
.chip {
  background: #1e3a5f;
  color: #e0f2fe;
  border: none;
  border-radius: 999px;
  padding: 4px 10px;
  font-size: 12px;
  cursor: pointer;
}
.chip:hover, .chip[aria-expanded="true"] { outline: 1px solid #38bdf8; }
.mode-toggle button {
  border: 1px solid #475569;
  background: transparent;
  color: #e2e8f0;
  border-radius: 999px;
  padding: 4px 12px;
  cursor: pointer;
}
.mode-toggle button.active {
  background: #22c55e;
  color: #052e16;
  border-color: #22c55e;
}
.api-base {
  width: 180px;
  padding: 4px 8px;
  border-radius: 6px;
  border: 1px solid #475569;
  background: #1e293b;
  color: #f8fafc;
}
.health { font-size: 12px; opacity: 0.9; }
.health.ok { color: #86efac; }
.health.bad { color: #fca5a5; }
.chip-detail {
  display: none;
  position: absolute;
  right: 18px;
  top: 58px;
  max-width: 420px;
  background: #1e293b;
  color: #e2e8f0;
  padding: 10px 12px;
  border-radius: 8px;
  font-size: 12px;
  z-index: 20;
  box-shadow: 0 8px 24px rgba(0,0,0,.35);
}
.chip-detail.open { display: block; }
.layout {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 14px;
  padding: 14px;
  max-width: 1400px;
  margin: 0 auto;
}
@media (max-width: 900px) {
  .layout { grid-template-columns: 1fr; }
}
.panel {
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 10px;
  padding: 14px;
}
.panel h2 {
  margin: 0 0 10px;
  font-size: 0.75rem;
  letter-spacing: 0.06em;
  text-transform: uppercase;
  color: var(--muted);
}
.upload-row, .ask-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 10px;
  align-items: center;
}
button.primary {
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 8px;
  padding: 8px 14px;
  cursor: pointer;
}
button.primary:disabled { opacity: 0.5; cursor: not-allowed; }
button.ghost {
  background: #f8fafc;
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 6px 10px;
  cursor: pointer;
}
.presets { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 10px; }
.presets button {
  border: none;
  background: #e0f2fe;
  color: #0c4a6e;
  border-radius: 999px;
  padding: 5px 10px;
  cursor: pointer;
  font-size: 12px;
}
.presets button.reject { background: #fee2e2; color: #7f1d1d; }
#query {
  flex: 1;
  min-width: 200px;
  padding: 10px 12px;
  border-radius: 8px;
  border: 1px solid var(--line);
}
.answer-box {
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 12px;
  min-height: 88px;
  margin-bottom: 10px;
  line-height: 1.5;
  white-space: pre-wrap;
}
.citation {
  border-left: 3px solid var(--accent);
  background: #f8fafc;
  padding: 8px 10px;
  margin-bottom: 6px;
  border-radius: 0 6px 6px 0;
  font-size: 13px;
}
.citation .meta { color: var(--muted); font-size: 12px; margin-bottom: 4px; }
.routes {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 8px;
  margin-bottom: 12px;
}
.route-col {
  background: #f8fafc;
  border: 1px solid var(--line);
  border-radius: 8px;
  padding: 8px;
  font-size: 12px;
}
.route-col h3 { margin: 0 0 6px; font-size: 12px; }
.route-col li { margin: 0 0 4px; list-style: none; }
.route-col ul { padding: 0; margin: 0; }
.eng {
  background: #0f172a;
  color: #e2e8f0;
  border-radius: 8px;
  padding: 10px;
  font-size: 12px;
}
.eng summary { cursor: pointer; font-weight: 600; }
.eng pre {
  overflow: auto;
  max-height: 240px;
  background: #020617;
  padding: 8px;
  border-radius: 6px;
}
.error-bar {
  display: none;
  margin: 0 14px;
  padding: 10px 12px;
  border-radius: 8px;
  background: #fef2f2;
  color: #991b1b;
  border: 1px solid #fecaca;
}
.error-bar.show { display: block; }
.footnote {
  max-width: 1400px;
  margin: 0 auto 24px;
  padding: 0 14px;
  color: var(--muted);
  font-size: 12px;
}
.muted { color: var(--muted); font-size: 12px; }
.spinner { opacity: 0.7; font-size: 13px; }
```

- [ ] **Step 2: 创建 `index.html`**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>PrismRAG Demo</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <header class="topbar">
    <div class="brand">
      <strong>PrismRAG</strong>
      <span>Industrial PDF RAG · Hybrid Demo</span>
    </div>
    <div class="top-right">
      <div id="metrics" class="metrics"></div>
      <div class="mode-toggle" role="group" aria-label="Mode">
        <button type="button" id="mode-demo" class="active" data-mode="demo">Demo</button>
        <button type="button" id="mode-live" data-mode="live">Live</button>
      </div>
      <input id="api-base" class="api-base" type="text" placeholder="API base (Live)" value="" title="Empty = same origin" />
      <span id="health" class="health">health: —</span>
    </div>
  </header>
  <div id="chip-detail" class="chip-detail" role="status"></div>
  <div id="error" class="error-bar" role="alert"></div>

  <main class="layout">
    <section class="panel" id="left">
      <h2>Query &amp; Answer</h2>
      <div class="upload-row">
        <input type="file" id="pdf-file" accept="application/pdf,.pdf" disabled />
        <button type="button" class="primary" id="btn-upload" disabled>Upload PDF</button>
        <label class="muted"><input type="checkbox" id="filter-doc" disabled /> 仅当前文档</label>
        <span id="upload-status" class="muted"></span>
      </div>
      <div id="presets" class="presets"></div>
      <div class="ask-row">
        <input id="query" type="text" placeholder="Ask about the industrial manual…" />
        <button type="button" class="primary" id="btn-ask">Ask</button>
        <span id="loading" class="spinner" hidden>Working…</span>
      </div>
      <div class="answer-box" id="answer"><span class="muted">答案将显示在这里</span></div>
      <div id="citations"></div>
    </section>

    <section class="panel" id="right">
      <h2>Retrieval Trace &amp; Engineer</h2>
      <div class="routes">
        <div class="route-col">
          <h3>BM25 <span class="muted">chunk</span></h3>
          <ul id="trace-bm25"></ul>
        </div>
        <div class="route-col">
          <h3>Dense <span class="muted">chunk</span></h3>
          <ul id="trace-dense"></ul>
        </div>
        <div class="route-col">
          <h3>Visual <span class="muted">page</span></h3>
          <ul id="trace-visual"></ul>
        </div>
      </div>
      <details class="eng" id="eng-panel">
        <summary>Engineer panel</summary>
        <div id="eng-meta" class="muted" style="margin:8px 0"></div>
        <pre id="eng-json">{}</pre>
      </details>
    </section>
  </main>
  <p id="footnote" class="footnote"></p>
  <script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 3: Commit**

```bash
git add static/demo/index.html static/demo/styles.css
git commit -m "feat(demo): add static page shell and styles"
```

---

### Task 3: `app.js` — Demo 模式全渲染

**Files:**
- Create: `static/demo/app.js`

- [ ] **Step 1: 实现完整 `app.js`（Demo + Live 骨架一次写全，Live 在本 Task 用不到的路径也要可调用）**

```javascript
// static/demo/app.js
(function () {
  "use strict";

  const state = {
    mode: "demo", // "demo" | "live"
    apiBase: "",
    health: null,
    query: "",
    loading: false,
    error: null,
    response: null,
    traceId: null,
    lastDocId: null,
    filterByDoc: false,
    fixtures: null,
    metrics: null,
  };

  const $ = (id) => document.getElementById(id);

  function showError(msg) {
    state.error = msg;
    const el = $("error");
    if (!msg) {
      el.classList.remove("show");
      el.textContent = "";
      return;
    }
    el.textContent = msg;
    el.classList.add("show");
  }

  function setLoading(on) {
    state.loading = on;
    $("loading").hidden = !on;
    $("btn-ask").disabled = on;
  }

  function baseUrl() {
    const raw = ($("api-base").value || "").trim().replace(/\/$/, "");
    return raw;
  }

  function apiUrl(path) {
    const b = baseUrl();
    if (!b) return path.startsWith("/") ? path : `/${path}`;
    return `${b}${path.startsWith("/") ? path : `/${path}`}`;
  }

  function renderMetrics() {
    const root = $("metrics");
    root.innerHTML = "";
    if (!state.metrics || !state.metrics.chips) return;
    state.metrics.chips.forEach((c) => {
      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = "chip";
      btn.textContent = `${c.label} ${c.value}`;
      btn.title = c.detail;
      btn.addEventListener("click", () => {
        const box = $("chip-detail");
        const open = box.classList.contains("open") && box.dataset.label === c.label;
        if (open) {
          box.classList.remove("open");
          return;
        }
        box.dataset.label = c.label;
        box.textContent = c.detail;
        box.classList.add("open");
      });
      root.appendChild(btn);
    });
    $("footnote").textContent = state.metrics.footnote || "";
  }

  function renderPresets() {
    const root = $("presets");
    root.innerHTML = "";
    if (!state.fixtures) return;
    state.fixtures.presets.forEach((p) => {
      const btn = document.createElement("button");
      btn.type = "button";
      btn.textContent = p.label || p.id;
      if ((p.id || "").includes("reject") || (p.label || "").toLowerCase().includes("out")) {
        btn.classList.add("reject");
      }
      btn.addEventListener("click", () => {
        $("query").value = p.query;
        ask();
      });
      root.appendChild(btn);
    });
  }

  function renderRouteList(el, items) {
    el.innerHTML = "";
    if (!items || !items.length) {
      el.innerHTML = "<li class='muted'>(empty)</li>";
      return;
    }
    items.forEach((it) => {
      const li = document.createElement("li");
      const score = typeof it.score === "number" ? it.score.toFixed(3) : it.score;
      li.textContent = `${it.chunk_id || "?"} · p${it.page_id} · ${score}`;
      el.appendChild(li);
    });
  }

  function renderCitations(citations) {
    const root = $("citations");
    root.innerHTML = "";
    if (!citations || !citations.length) {
      root.innerHTML = "<p class='muted'>No citations</p>";
      return;
    }
    citations.forEach((c, i) => {
      const div = document.createElement("div");
      div.className = "citation";
      const page = c.page_number != null ? c.page_number : c.page_id;
      div.innerHTML =
        `<div class="meta"><b>C${i + 1}</b> · page ${page} · ` +
        `${c.doc_id || "—"} · <code>${c.chunk_id || ""}</code></div>` +
        `<div>${escapeHtml(c.snippet || "")}</div>`;
      root.appendChild(div);
    });
  }

  function escapeHtml(s) {
    return String(s)
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/"/g, "&quot;");
  }

  function renderResponse(resp, traceId) {
    state.response = resp;
    state.traceId = traceId || (resp && resp._demo_trace_id) || null;
    $("answer").textContent = (resp && resp.answer) || "(empty answer)";
    renderCitations(resp && resp.citations);
    const rt = (resp && resp.retrieval_trace) || {};
    renderRouteList($("trace-bm25"), rt.bm25_top5);
    renderRouteList($("trace-dense"), rt.dense_top5);
    renderRouteList($("trace-visual"), rt.visual_top5);

    const sr = (resp && resp.self_rag) || { enabled: false };
    const cr = (resp && resp.crag) || { enabled: false };
    $("eng-meta").innerHTML =
      `<div>Trace-Id: <code>${escapeHtml(state.traceId || "—")}</code>` +
      (state.traceId && state.mode === "live"
        ? ` · <a href="${apiUrl("/trace/" + state.traceId)}" target="_blank" rel="noopener">open /trace</a>`
        : "") +
      `</div>` +
      `<div>self_rag.enabled=${sr.enabled}` +
      (sr.passed != null ? ` passed=${sr.passed}` : "") +
      (sr.score != null ? ` score=${sr.score}` : "") +
      (sr.final_action ? ` action=${escapeHtml(String(sr.final_action))}` : "") +
      `</div>` +
      `<div>crag.enabled=${cr.enabled}` +
      (cr.applied != null ? ` applied=${cr.applied}` : "") +
      (cr.final_action ? ` action=${escapeHtml(String(cr.final_action))}` : "") +
      `</div>`;
    const dump = Object.assign({}, resp || {});
    delete dump._demo_trace_id;
    $("eng-json").textContent = JSON.stringify(dump, null, 2);
  }

  function setMode(mode) {
    state.mode = mode;
    $("mode-demo").classList.toggle("active", mode === "demo");
    $("mode-live").classList.toggle("active", mode === "live");
    const live = mode === "live";
    $("pdf-file").disabled = !live;
    $("btn-upload").disabled = !live;
    $("filter-doc").disabled = !live || !state.lastDocId;
    if (live) {
      checkHealth();
    } else {
      $("health").textContent = "health: (demo)";
      $("health").className = "health";
    }
  }

  async function loadData() {
    const [fx, mx] = await Promise.all([
      fetch("fixtures.json").then((r) => {
        if (!r.ok) throw new Error("fixtures.json " + r.status);
        return r.json();
      }),
      fetch("metrics.json").then((r) => {
        if (!r.ok) throw new Error("metrics.json " + r.status);
        return r.json();
      }),
    ]);
    state.fixtures = fx;
    state.metrics = mx;
    renderMetrics();
    renderPresets();
  }

  async function askDemo(query) {
    const resp = state.fixtures.responses[query];
    if (!resp) {
      throw new Error("Demo 模式仅支持预设问句（请点上方 chips，或切换 Live）");
    }
    // 深拷贝避免后续污染
    return JSON.parse(JSON.stringify(resp));
  }

  async function askLive(query) {
    const body = {
      query,
      k: 5,
      use_rerank: true,
    };
    if (state.filterByDoc && state.lastDocId) {
      body.doc_id = state.lastDocId;
    }
    const r = await fetch(apiUrl("/ask"), {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(body),
    });
    const traceId = r.headers.get("X-Trace-Id") || r.headers.get("x-trace-id");
    let data;
    try {
      data = await r.json();
    } catch (_) {
      throw new Error("Live /ask 返回非 JSON，HTTP " + r.status);
    }
    if (!r.ok) {
      const detail = data.detail || JSON.stringify(data);
      throw new Error("Live /ask " + r.status + ": " + detail);
    }
    return { data, traceId };
  }

  async function ask() {
    const query = ($("query").value || "").trim();
    if (!query) {
      showError("请输入问题");
      return;
    }
    showError(null);
    setLoading(true);
    try {
      if (state.mode === "demo") {
        const data = await askDemo(query);
        renderResponse(data, data._demo_trace_id || null);
      } else {
        const { data, traceId } = await askLive(query);
        renderResponse(data, traceId);
      }
    } catch (e) {
      showError(e.message || String(e));
    } finally {
      setLoading(false);
    }
  }

  async function checkHealth() {
    const el = $("health");
    el.textContent = "health: …";
    el.className = "health";
    try {
      const r = await fetch(apiUrl("/health"));
      if (!r.ok) throw new Error("HTTP " + r.status);
      const j = await r.json();
      state.health = j;
      el.textContent = `health: ok · pages=${j.index_pages ?? "—"}`;
      el.classList.add("ok");
    } catch (e) {
      state.health = null;
      el.textContent = "health: fail (" + (e.message || e) + ")";
      el.classList.add("bad");
    }
  }

  async function uploadPdf() {
    if (state.mode !== "live") return;
    const fileInput = $("pdf-file");
    const file = fileInput.files && fileInput.files[0];
    if (!file) {
      showError("请选择 PDF 文件");
      return;
    }
    if (!/\.pdf$/i.test(file.name)) {
      showError("仅支持 PDF");
      return;
    }
    showError(null);
    $("btn-upload").disabled = true;
    $("upload-status").textContent = "uploading…";
    try {
      const fd = new FormData();
      fd.append("file", file, file.name);
      const r = await fetch(apiUrl("/ingest"), { method: "POST", body: fd });
      let data;
      try {
        data = await r.json();
      } catch (_) {
        throw new Error("ingest 非 JSON HTTP " + r.status);
      }
      if (!r.ok) {
        throw new Error("ingest " + r.status + ": " + (data.detail || JSON.stringify(data)));
      }
      state.lastDocId = data.doc_id;
      $("filter-doc").disabled = false;
      $("upload-status").textContent =
        `doc_id=${data.doc_id} · pages=${data.num_pages} · chunks=${data.num_chunks}`;
    } catch (e) {
      showError(e.message || String(e));
      $("upload-status").textContent = "upload failed";
    } finally {
      $("btn-upload").disabled = false;
    }
  }

  function wire() {
    $("mode-demo").addEventListener("click", () => setMode("demo"));
    $("mode-live").addEventListener("click", () => setMode("live"));
    $("btn-ask").addEventListener("click", ask);
    $("query").addEventListener("keydown", (ev) => {
      if (ev.key === "Enter") ask();
    });
    $("btn-upload").addEventListener("click", uploadPdf);
    $("filter-doc").addEventListener("change", (ev) => {
      state.filterByDoc = !!ev.target.checked;
    });
    $("api-base").addEventListener("change", () => {
      if (state.mode === "live") checkHealth();
    });
    document.addEventListener("click", (ev) => {
      if (!ev.target.classList.contains("chip")) {
        /* keep detail until chip click toggles */
      }
    });
  }

  async function main() {
    wire();
    setMode("demo");
    try {
      await loadData();
    } catch (e) {
      showError("加载 fixtures/metrics 失败: " + (e.message || e));
    }
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", main);
  } else {
    main();
  }
})();
```

- [ ] **Step 2: 本地用任意静态服务器快速目视（可选，无需 GPU）**

```bash
cd /Users/theyang/Documents/ai/pdf-rag/static/demo
python3 -m http.server 8765
# 浏览器打开 http://127.0.0.1:8765/ → 点 preset chips
```

Expected: 答案、引用、三路列表、工程面板 JSON 更新；拒答样例 citations 为空。

- [ ] **Step 3: Commit**

```bash
git add static/demo/app.js
git commit -m "feat(demo): implement Demo/Live client logic"
```

---

### Task 4: FastAPI 挂载 `/demo` + 烟测

**Files:**
- Modify: `src/api/routes.py`（文件**末尾**，所有 `@app` 路由之后）
- Modify: `tests/test_api.py`

- [ ] **Step 1: 写失败烟测**

在 `tests/test_api.py` 末尾追加：

```python
def test_demo_static_index_served():
    """Demo 静态页由 StaticFiles 挂载；不依赖真实 retriever 重模型。"""
    routes.set_retriever(_fake_retriever())
    c = TestClient(routes.app)
    r = c.get("/demo/")
    # StaticFiles html=True 可能 200 于 /demo/ 或需 /demo/index.html
    if r.status_code == 404:
        r = c.get("/demo/index.html")
    assert r.status_code == 200
    assert "PrismRAG" in r.text
    # 附属资源
    r2 = c.get("/demo/app.js")
    assert r2.status_code == 200
    r3 = c.get("/demo/fixtures.json")
    assert r3.status_code == 200
```

- [ ] **Step 2: 跑测应失败**

```bash
.venv/bin/python -m pytest tests/test_api.py::test_demo_static_index_served -v
```

Expected: FAIL 404

- [ ] **Step 3: 在 `routes.py` 末尾挂载（路径相对仓库根，兼容 cwd）**

在文件顶部 imports 区确保有（若已有 `Path` 则不重复）：

```python
from pathlib import Path
from fastapi.staticfiles import StaticFiles
```

在 **所有路由函数定义之后**（`ask` 函数结束之后）追加：

```python
def _demo_static_dir() -> Path:
    """static/demo：优先仓库根（与 scripts/run_api.py cwd 一致），否则相对本文件回溯。"""
    candidates = [
        Path.cwd() / "static" / "demo",
        Path(__file__).resolve().parents[2] / "static" / "demo",
    ]
    for p in candidates:
        if p.is_dir():
            return p
    return candidates[0]


_DEMO_DIR = _demo_static_dir()
if _DEMO_DIR.is_dir():
    app.mount(
        "/demo",
        StaticFiles(directory=str(_DEMO_DIR), html=True),
        name="demo",
    )
else:
    logger.warning("Demo static dir missing: %s", _DEMO_DIR)
```

**注意：**
- `mount` 必须在 API 路由**之后**注册（Starlette 匹配顺序）。
- **不要**把 `/` 指到 demo，以免破坏 OpenAPI/健康检查习惯；入口固定 `/demo/`。
- 本 Task **不**加 CORS：同源 `/demo` + 相对 `/ask` 足够；跨 origin 用 `api-base` 时若浏览器拦，再开可选 CORS（见 Task 5 可选步）。

- [ ] **Step 4: 跑测**

```bash
.venv/bin/python -m pytest tests/test_api.py tests/test_demo_fixtures.py -q
```

Expected: PASS（含 demo 烟测与既有 ask/ingest 测）

- [ ] **Step 5: Commit**

```bash
git add src/api/routes.py tests/test_api.py
git commit -m "feat(api): mount static PrismRAG demo at /demo"
```

---

### Task 5: Live 路径验收 + 可选 CORS + README 入口

**Files:**
- Modify: `src/api/routes.py`（仅当需要 CORS）
- Modify: `README.md`（短节）
- Optional: `Makefile` 增加 `demo` 目标

- [ ] **Step 1: 可选 CORS（仅当 `PRISMRAG_CORS_ORIGINS` 非空）**

在 `app = FastAPI(...)` 之后、`ObservabilityMiddleware` 附近加入：

```python
import os
from fastapi.middleware.cors import CORSMiddleware

_cors = os.environ.get("PRISMRAG_CORS_ORIGINS", "").strip()
if _cors:
    app.add_middleware(
        CORSMiddleware,
        allow_origins=[o.strip() for o in _cors.split(",") if o.strip()],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
        expose_headers=["X-Trace-Id"],
    )
```

默认不设置环境变量 → 不启用 CORS。

- [ ] **Step 2: README 增加 Demo 小节**（放在「系统概览」附近或本地运行处）

```markdown
### Web Demo（Hybrid）

```bash
# 仓库根启动 API 后打开
python scripts/run_api.py
# 浏览器: http://127.0.0.1:8000/demo/
# 默认 Demo 模式（fixtures，无 GPU）；Live 需索引 + 模型就绪
# 跨域 Live: PRISMRAG_CORS_ORIGINS=http://127.0.0.1:8765
```
```

- [ ] **Step 3:（可选）Makefile**

```makefile
demo: ## 启动 API 并提示 Demo URL（需依赖与索引按需准备）
	@echo "Open http://127.0.0.1:8000/demo/ after server starts"
	python scripts/run_api.py
```

- [ ] **Step 4: 手工 Live 检查清单（有 API 时）**

1. Demo 模式点 5 个 chips，Gate2 sample 工程面板 `self_rag.enabled=true`
2. 切 Live，`api-base` 留空，health 应变 ok（假 retriever 环境除外）
3. 真服务：ask 后工程面板 Trace-Id 非空；`/trace/{id}` 链接可点
4. 上传 `tests/fixtures/sample.pdf`（若存在）或任意小 PDF → 显示 doc_id
5. 勾选「仅当前文档」再 ask

- [ ] **Step 5: 全量轻测**

```bash
.venv/bin/python -m pytest tests/test_api.py tests/test_demo_fixtures.py -q
```

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/api/routes.py README.md Makefile
git commit -m "docs: document /demo entry; optional CORS for Live"
```

---

### Task 6: handoff 与收尾

**Files:**
- Modify: `handoff.md`（顶栏简短交付摘要）

- [ ] **Step 1: 在 handoff 顶部增加 Demo 交付摘要**

```markdown
## 0″. Demo 页（feat/demo-page）

| 项 | 内容 |
|----|------|
| **入口** | `http://host:8000/demo/` |
| **模式** | Demo fixtures 默认 · Live `/ask`+`/ingest` |
| **代码** | `static/demo/*` · mount in `src/api/routes.py` |
| **Spec/Plan** | `docs/superpowers/specs/2026-07-24-demo-page-design.md` · `plans/2026-07-24-demo-page.md` |
| **测** | `tests/test_demo_fixtures.py` · `test_demo_static_index_served` |
```

- [ ] **Step 2: 最终自检命令**

```bash
.venv/bin/python -m pytest tests/test_api.py tests/test_demo_fixtures.py -q
ls -la static/demo/
```

- [ ] **Step 3: Commit**

```bash
git add handoff.md
git commit -m "docs: handoff note for /demo hybrid page"
```

---

## Spec coverage checklist（计划自检）

| Spec 要求 | Task |
|-----------|------|
| Hybrid Showcase + 工程师面板 | T2 DOM + T3 `details#eng-panel` |
| 静态单页 HTML/CSS/JS | T2–T3 |
| Fixture 默认 + Live 开关 | T3 `setMode` / `askDemo` / `askLive` |
| 左右分栏 | T2 `.layout` grid |
| 问答 + citations | T3 `renderResponse` / `renderCitations` |
| 预设问句 ≥4 + 拒答 | T1 fixtures |
| 三路 retrieval_trace | T3 `renderRouteList` ×3 |
| 评测角标 | T1 metrics + T3 `renderMetrics` |
| PDF 上传 + doc_id 过滤 | T3 `uploadPdf` / `filterByDoc` |
| StaticFiles `/demo` | T4 |
| 零 AskResponse schema 变更 | 全程不改 Pydantic 模型 |
| 可选 CORS | T5 |
| 契约/烟测 | T1 + T4 |
| 窄屏折列 | T2 `@media (max-width: 900px)` |
| 不做纯 `/search` | 无相关代码 |

**Placeholder scan:** 无 TBD；Live 真服务依赖环境写在手工清单，不阻塞 Demo 离线路径。

**Type/field consistency:** `bm25_top5` / `dense_top5` / `visual_top5`、`citations[].snippet|page_id|page_number`、`X-Trace-Id` 与 `src/api/routes.py` + middleware 一致。

---

## 执行方式（完成后由用户选）

Plan complete and saved to `docs/superpowers/plans/2026-07-24-demo-page.md`.

**1. Subagent-Driven（推荐）** — 每 Task 新 subagent，Task 间 review  
**2. Inline Execution** — 本会话按 `executing-plans` 连续做完  

Which approach?
